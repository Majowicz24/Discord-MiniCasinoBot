import discord
from discord.ext import commands

PREFIX = '-'
bot = commands.Bot(command_prefix=PREFIX, description="Gambling Bot")
client = commands.Bot(command_prefix="-")
TOKEN = 'YOUR_TOKEN_HERE'
